#!/bin/bash

set -o errexit
set -o nounset


worker_ready(){
    celery -A backend.celery_app inspect ping
}

until worker_ready; do 
    >&2 echo 'Waiting for Celery workers to become available...'
    sleep 1
done

>&2 echo 'Celery workers are available and ready! Starting Flower...'

exec celery -A backend.celery_app --broker="${CELERY_BROKER}" flower \
    --app=backend.celery_app \
    --port=5555
